// Update this page (the content is just a fallback if you fail to update the page)

import React from 'react';
import { EliteAIPlatform } from '@/components/elite-ai-platform';

const Index = () => {
  return <EliteAIPlatform />;
};

export default Index;
